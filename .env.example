# === REQUIRED ===

# Database (choose one)
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/db  # Production
# DATABASE_URL=sqlite+aiosqlite:///./payment.db  # Development only

# Security (CHANGE IN PRODUCTION!)
WEBHOOK_SECRET=your_super_secret_min_32_chars  # Generate: openssl rand -hex 32

# === OPTIONAL ===

# Email (if not set, OTP printed to console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your_app_password  # For Gmail: https://support.google.com/accounts/answer/185833

# Currency settings (default: USD/$)
CURRENCY_CODE=USD
CURRENCY_SYMBOL=$